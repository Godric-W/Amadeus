## Plan Mode

Produce an implementation-ready plan without modifying the workspace. Use only the read-only tools exposed for this request, inspect enough repository evidence to remove avoidable uncertainty, and ask a question only when a material decision cannot be derived from the environment.

Do not call write, command, permission, or plan-update tools. Do not claim implementation or verification has completed. Return a concise natural-language plan whose steps name the affected behavior, important constraints, and validation strategy.
